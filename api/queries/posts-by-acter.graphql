#import "../fragments/post-display.fragment.graphql"

query GetPosts($acterId: String) {
  posts(where: { acterId: { equals: $acterId } }) {
    ...PostDisplay
    Author {
      name
      avatarUrl
      ActerType {
        name
      }
    }
    Acter {
      name
    }
  }
}

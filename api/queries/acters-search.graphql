#import "../fragments/acter-display.fragment.graphql"

query ActersSearch($searchText: String) {
  acters(
    where: {
      AND: [
        { ActerType: { isNot: { name: { in: ["user", "activity"] } } } }
        { name: { contains: $searchText, mode: insensitive } }
      ]
    }
  ) {
    ...ActerDisplay
  }
}

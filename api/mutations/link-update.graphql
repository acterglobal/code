#import "../fragments/link-display.fragment.graphql"

mutation UpdateLink($id: String!, $name: String, $url: String) {
  updateLink(
    where: { id: $id }
    data: { name: { set: $name }, url: { set: $url } }
  ) {
    ...LinkDisplay
  }
}

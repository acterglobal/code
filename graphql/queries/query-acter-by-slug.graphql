query acterByActerTypeSlug($acterTypeId: String!, $slug: String!) {
  getActer(acterTypeId: $acterTypeId, slug: $slug) {
    name
    description
    slug
    location
    url
    avatarUrl
    bannerUrl
  }
}

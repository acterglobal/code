mutation CreateActer(
  $name: String!
  $description: String
  $acterTypeId: String!
) {
  createActer(
    name: $name
    description: $description
    acterTypeId: $acterTypeId
  ) {
    id
    name
    description
    slug
    location
    url
    ActerType {
      name
    }
  }
}

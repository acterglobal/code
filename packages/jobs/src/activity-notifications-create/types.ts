import { ActivityPick } from '../activity-notifications-on-acter-create'

export interface ActivityNotification {
  /**
   * The activity for which we are creating a notification
   */
  activity: ActivityPick
}

#import "../fragments/post-display.fragment.graphql"

query GetPost($id: String!) {
  post(where: { id: $id }) {
    ...PostDisplay
    Comments {
      ...PostDisplay
    }
    Parent {
      id
    }
  }
}

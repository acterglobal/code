mutation CreateInvite($email: String!, $message: String!, $onActerId: String!) {
  createInviteCustom(email: $email, message: $message, onActerId: $onActerId) {
    email
    message
    OnActer {
      id
      name
    }
    createdByUser {
      id
      name
    }
  }
}

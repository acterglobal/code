import { Meta, Story } from '@storybook/react'
import { SearchActivities } from 'src/components/activity/search-activities'

export default {
  title: 'Search/Activities',
  component: SearchActivities,
} as Meta

export const Activities: Story = () => <SearchActivities />

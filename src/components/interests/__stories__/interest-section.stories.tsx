import { Story, Meta } from '@storybook/react'
import InterestsSection from '../interests-section'

export default {
  title: 'interests/Interests',
  component: InterestsSection,
} as Meta

export const Default: Story = () => <InterestsSection />
